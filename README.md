# SchafkopfTurnier (offline)

Offline-Flask-App zur Verwaltung von Schafkopfturnieren (SQLite, Backup, CSV).

## Setup (macOS/Linux)
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python run.py